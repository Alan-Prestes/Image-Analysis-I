ExpImage-package        ExpImage: Experimental Image Analysis Tools
Normatiza               Normatiza a matriz para que os valores variem
                        entre valores pre-estabelecidos.
as_image                Transform matrix into image (Transformar matriz
                        em imagem).
calibration_palette     Function to create a color palette for image
                        color calibration (Funcao para criar uma paleta
                        de cores para calibracao das cores de imagens).
classify_image          Filter values in an array or image (Filtrar
                        valores em uma matriz ou imagem).
clustering_Kmeans       Image segmentation by clustering using the
                        Kmeans method (Segmentacao de imagens pela
                        clusterização pelo metodo Kmeans).
color_pallete           Creates a color palette from an image (Criar
                        uma paleta de cores a partir de uma imagem).
compare_image           Function to compare image sizes (Funcao para
                        comparar os tamanhos das imagens).
contour_image           Contorno da imagem segmentada (Outline of the
                        segmented image.)
convert_color           Convert colors (Converter cores).
create_png              Create a png image with a transparent
                        background (Criar uma imagem png com o fundo
                        transparente)
crop_image              Esta funcao corta a imagem retirando as
                        laterais nao desejadas. (This function removes
                        unwanted sides from the images.)
dilate_image            Funcao para acrescentar bordas nos objetos.
edit_image              This function changes the brightness, contrast
                        and makes the gamma adjustment in the image
                        (Esta funcao permite mudar o brilho, contraste
                        e fazer um ajuste gamma na imagem).
edit_imageGUI           This function changes the brightness, contrast
                        and makes the gamma adjustment in the image
                        (Esta funcao permite mudar o brilho, contraste
                        e fazer um ajuste gamma na imagem).
erode_image             Funcao para eliminar as bordas dos objetos.
example_image           Images used as an example (Banco de imagens
                        utilizadas como exemplo).
exclude_object          Exlude objects in an image by encircling them
                        or using a mask (Excluir objetos em uma imagem
                        circundado-os ou usando uma mascara).
extract_pixels          Function to exclude pixels from the background
                        or foreground in an image (Funcao para excluir
                        em uma imagem os pixels correspondente ao
                        background ou foreground)
getwd_script            Obter o diretorio onde esta o script. (Get the
                        script directory.)
gray_scale              Function to get a grayscale image from a color
                        image (Funcao para a obtencao de uma imagem em
                        escala de cinza a partir de uma imagem
                        colorida)
histogram_image         Create a histogram from image bands (Criar um
                        histrograma a partir das bandas das imagens)
index_bands             Estimation of vegetation indices (Estimacao de
                        indices de vegetacao).
info_image              Image information (Informacoes sobre as
                        imagens).
join_bands              Function to compare image sizes (Funcao para
                        comparar os tamanhos das imagens).
join_image              Join images(Juntar imagens)
linearize_image         Image linearization (Linearizacao da imagem).
mask_pixels             Creates a mask over the background or
                        foreground (Cria uma mascara sobre o background
                        ou foreground)
mask_raster             Creates a mask over the background or
                        foreground in a raster image (Cria uma mascara
                        sobre o background ou foreground em uma imagem
                        no formato raster)
measure_image           Function to obtain measurements associated with
                        objects in binary images (Funcao para obter
                        medidas associadas aos objetos em imagens
                        binarias).
normalize_image         Create a histogram from image bands (Criar um
                        histrograma a partir das bandas das imagens)
pick_circle             Segmentation with the shape of a circle by
                        clicking on the image (Segmentacao com o
                        formato de um circulo clicando em na imagem)
pick_color              Selecionar os valores de RGB em pixels
                        (Selecting RGB values in pixels).
pick_count              Count objects (Contar objetos).
pick_length             Measure objects in the image on click (Medir
                        objetos na imagem ao clicar).
pick_palette            Obter uma paleta de cores com cliques (Get a
                        color palette with clicks ).
pick_resolution         Get the dpi resolution on an image (Obter a
                        resolução em dpi em uma imagem).
pick_segmentation       Segmenting objects manually (Segmentação de
                        objetos manualmente)
plot_image              Esta funcao plota imagens. (This function plot
                        the images.)
plot_indexes            Function to plot multiple indices (funcao para
                        plotar varios indices)
plot_meansures          Funcao para sobrepor informacoes sobre os
                        objetos da imagem
predict_RF              Pixel classification by Random Forest
                        methodology (Classificacao de pixels pela
                        metodologia Random Forest)
predict_logit           Segmentacao a partir de modelo logit
                        (Segmentation from the logit model)
raster2image            Convert a image in raster format to ExpImage
                        format (Converte uma imagem no formato raster
                        para o formato ExpImage)
read_image              Function to import an image (Funcao para
                        importa uma imagem).
resize_image            Function to change image size (Funcao para
                        mudar o tamanho da imagem)
rotate_image            Esta funcao rotaciona a imagem. (This function
                        rotates the image .)
segmentation            Image segmentation function (Funcao para a
                        segmentacao de imagens).
segmentation_RF         Image segmentation by Random Forest
                        (Segmentacao de imagens por Random Forest).
segmentation_logit      Funcao para a segmentacao de imagens.
segmentation_logitGUI   Funcao para a segmentacao de imagens.
segmentation_steps      Image segmentation function (Funcao para a
                        segmentacao de imagens).
setwd_script            Definir o diretório do script working directory
                        (Set the script Directory as working
                        directory).
shape_file.BorderExtract
                        Excluir as bordas do objetos/parcelas do
                        shapefile
shape_file.create       Funcao para criar as coordenadas dos
                        objetos/parcelas na imagem
shape_file.plot         Plotar os objetos/parcelas do shapefile na
                        imagem
shape_file.split        Divide a imagem em objetos/parcelas de acordo
                        com o shapefile
skeletonize_image       Function for skeletonization in images (Esta
                        funcao faz a skeletonizacao em imagens).
split_image             This function splits the image into several
                        others (Esta funcao divide a imagem em varias
                        outras de acordo com os objetos que contem)
square_image            Transform the square image (Transforme a imagem
                        quadrada).
summary_color           Summarize the colors present in the segmented
                        area of an image (Sumarizar as cores presentes
                        na area segmentada de uma imagem).
thinning_image          This function makes the thinning images(Esta
                        funcao faz o thinning em imagens).
tif_info                Image information in tif format (Informacoes
                        das imagens no formato tif).
write_image             Function to save an image (Funcao para salvar
                        uma imagem).
